#include <iostream>

using namespace std;

int K;

int main() {
    int T;
    scanf("%d", &T);
    for (int test_case = 1; test_case <= T; ++test_case) {
        scanf("%d", &K);
        for (int i = 0; i < K; ++i) {
            s
        }
        printf("#%d ", test_case);
    }
    return 0;
}
