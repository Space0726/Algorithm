#include <iostream>
#include <vector>

using namespace std;
vector<vector<int>> plates;
void move_plate() {

}

int main() {
    int n;
    cin >> n;
    plates = vector<vector<int>> (3, vector<int>(n));

    return 0;
}
